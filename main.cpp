#include <iostream>
#include "my_deque.h"

int main() {
    my_deque<int> qq;
    for (int i = 0; i < 10; i++) {
        if (i % 2) qq.push_back(i);
        else
            qq.push_front(i);
        for (my_deque<int>)
    }
    return 0;
}